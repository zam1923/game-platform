import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gameplatform.app',
  appName: 'Game Platform',
  webDir: 'dist',
  android: {
    backgroundColor: '#0f0f13',
  },
};

export default config;
